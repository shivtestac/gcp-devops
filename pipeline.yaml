apiVersion: deploy.cloud.google.com/v1
kind: DeliveryPipeline
metadata:
  name: myapp-pipeline
serialPipeline:
  stages:
  - targetId: dev
    profiles: ["dev"]
  - targetId: prod
    profiles: ["prod"]
